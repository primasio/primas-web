import template from './whitepaper.html';
import controller from './whitepaper.controller';

let careerComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default careerComponent;
