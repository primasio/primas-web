import template from './face.html';
import controller from './face.controller';
import './face.scss';

let faceComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default faceComponent;
